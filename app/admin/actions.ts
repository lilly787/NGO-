"use server";
import {redirect} from "next/navigation";import {revalidatePath} from "next/cache";import {isAdmin} from "@/lib/auth";import {prisma} from "@/lib/prisma";import {cleanContent,toSlug,Kind} from "@/lib/cms";import {ContentStatus} from "@prisma/client";
async function allowed(){if(!await isAdmin())redirect("/admin/login")}
export async function save(kind:Kind,id:string|undefined,form:FormData){await allowed();const title=String(form.get("title")||"").trim();const status:ContentStatus=form.get("status")==="PUBLISHED"?ContentStatus.PUBLISHED:ContentStatus.DRAFT;const data={title,slug:toSlug(title),featuredImage:String(form.get("featuredImage")||"")||null,content:cleanContent(String(form.get("content")||"")),status,publishedAt:status===ContentStatus.PUBLISHED?new Date():null};if(!title||!data.content)throw new Error("Title and content are required.");if(kind==="news"){if(id)await prisma.news.update({where:{id},data});else await prisma.news.create({data})}else{const voiceData={...data,authorName:String(form.get("authorName")||"").trim(),authorRole:String(form.get("authorRole")||"")||null,references:String(form.get("references")||"")||null};if(!voiceData.authorName)throw new Error("Author name is required.");if(id)await prisma.voice.update({where:{id},data:voiceData});else await prisma.voice.create({data:voiceData})}revalidatePath("/news");revalidatePath("/gsei-voices");redirect(`/admin/${kind==="news"?"news":"voices"}`)}
export async function remove(kind:Kind,id:string){await allowed();if(kind==="news")await prisma.news.delete({where:{id}});else await prisma.voice.delete({where:{id}});revalidatePath("/news");revalidatePath("/gsei-voices");redirect(`/admin/${kind==="news"?"news":"voices"}`)}
